---
- name: Tests
  hosts: ipaserver
  become: true
  gather_facts: false

  tasks:
  - ipavaultcontainer:
      ipaadmin_password: MyPassword123
      username: user01
      users:
      - user01
      - user02
      - user03
      groups:
      - testusers
      services:
      - "HTTP/{{ groups.ipaserver[0] }}"
      action: member
